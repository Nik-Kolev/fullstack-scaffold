POSTGRES_USER=devuser
POSTGRES_PASSWORD=devpass
POSTGRES_DB=scaffold

# Optional — point the containerized server/worker at an external database or
# Redis instance instead of the bundled postgres/redis containers below.
# Overrides the default (the bundled containers) when set.
# DATABASE_URL=
# REDIS_URL=
